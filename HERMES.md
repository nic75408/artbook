# artbook — 项目手册

这份文件会自动注入每个在本仓库工作的 agent 的系统提示词。
**接卡先读这里**，尤其是「部署事实」和「踩过的坑」两节——里面每一条都是
真实事故换来的，违反其中任何一条，你的交付会在线上坏掉而在本地全绿。

维护规则：新踩的坑当轮补进「踩过的坑」；赤拔的新拍板补进「已定的决策」。
改这份文件跟改代码一样走卡、走审核。

---

## 这是什么

**艺术手册（artbook）** —— 一个每日一件艺术品的移动端 PWA。
赤拔（老板，设计师 + 产品经理）本人是唯一用户，**他在 iPhone 上从主屏图标
启动使用**，对真实移动端体验极其敏感。

- 线上地址：https://nic75408.github.io/artbook/
- 托管：GitHub Pages，`main` 分支即生产环境
- 内容来源：克利夫兰美术馆 / 大都会博物馆 开放数据（`pipeline/`）

**判断一个交付好不好，标准只有一个：赤拔在 iPhone 上打开，看到的是不是
预期的样子。** 技术指标全绿而画面是残的，等于没做——这种事今天已经发生过。

---

## 部署事实（违反即线上崩，本地测不出来）

1. **站点部署在 `/artbook/` 子路径下，不是域名根。**
   所以**禁止一切根相对路径**（`/icons/...`、`/js/...`、`/data/...`）。
   一律用相对路径（`icons/...`）或以 `import.meta.url` 为基准解析。
   本地起 `python3 -m http.server` 时仓库根 = 域名根，子路径问题被完全掩盖
   ——**本地全绿不代表线上好**。要验就用真线上地址，或把仓库软链成
   `<临时目录>/artbook` 再起服务器。

2. **路由是 hash 式的**（`js/router.js`，`location.hash = hash`，无 pushState）。
   所以相对路径在所有路由下都安全，base URL 恒为 `/artbook/`。
   **若将来改成 pushState 路由，所有相对资源路径都会崩** ——改路由必须
   同时改资源解析方式。

3. **Service Worker 有缓存版本号**（`sw.js` 的 `artbook-app-v*`）。
   改了任何静态资源就必须升版本号，否则老用户拉到的是旧文件。
   赤拔那边验证前需要「删除旧 PWA → 重新安装」才能清掉旧 SW。

4. **画作图片来自外部 CDN**（`openaccess-cdn.clevelandart.org` 等），
   离线场景下必然加载不出来，离线验收只看缓存的页面骨架和 `offline.html`。

---

## 已定的决策（赤拔拍板过的，不要重新讨论）

- **设计系统以 `DESIGN.md` 为唯一准绳**（Google design.md token 格式，自带
  校验器 `npx @google/design.md lint`）。色值、字阶、间距、版心一律引用
  其中的 token，不自创数值。
- 视觉调性：美术馆导览风。衬线中文（Songti SC）、暖纸底色、金色点缀。
- 图标规范在 `icons/ICON-SPEC.md`，图标组件在 `js/icons/Icon.js`。
- 单屏独占：一屏一件作品，画框 + 题名总高不溢出 `100dvh`。

---

## 踩过的坑（每条都是真实事故）

1. **`import` 的东西对方没 `export` → 整页白屏。**
   `feed.js` 从 `ui.js` 导入 `Icon`，而 `ui.js` 没导出它——ES module 导入失败
   会让**整个模块图停止执行**，`#view` 元素存在但为空。
   表现极具欺骗性：背景色正常、`DOMContentLoaded` 只要几十毫秒、
   HTTP 全 200，**但页面是空白的**。
   教训：改动模块导出/导入后，必须在浏览器里看首屏有没有真实内容，
   `curl` 拿得到文件不代表浏览器能执行。

2. **根相对路径让线上所有图标 404。**（见「部署事实」1）
   26 个图标文件都在 main 上、审核全过、卡也 done，线上一个都渲染不出来。

3. **同步渲染撞异步预加载 → 图标永远是占位圆圈。**
   `Icon()` 是同步函数，缓存没命中就返回占位符且**永不重渲染**；
   `preloadIcons()` 是异步的。首屏渲染跑在预加载之前，第一眼必然是占位。
   而且「UI 用到但没进 `CRITICAL_ICONS` 清单」的图标永远是占位。
   教训：**验收必须验「首次冷加载」**——硬刷新后不做任何导航就看。
   靠翻页后的重渲染验收，会把这类竞态整个掩盖掉。

4. **交付物没 commit 进 worktree 分支 = 零交付。**
   任务结束时 scratch 工作区会被整删；未提交的修改也拿不到。
   设计规格尤其致命：`DESIGN.md` 是设计师传递规格给工程的唯一物理通道，
   文件没落盘，后面的工程卡就无从做起。

5. **普通卡不许碰 `main`，只有标题以「发布」开头的卡可以。**
   未审先推的后果不止那一次推送——它让 `main` 的历史变得不可信：
   「这张卡的分支合进 main 了」从此成为假阳性信号。

6. **`data/index.json` 只能由每日出刊流水线修改，功能发布分支禁止携带其改动合并。**
   出刊流水线每日更新 `data/index.json` 的 `latest` 字段和 `issues` 列表（如 `2026-08-31`）。
   功能分支（布局修复、PWA icon 等）在开发周期内不更新该文件，合并时若直接合入会
   用旧版本的索引覆盖最新一期——921b78a 与 ebb4713 的覆盖事故即因此发生。
   **约定**：
   - `data/index.json.latest` 和 `data/index.json.issues` 只能由出刊流水线（pipeline）更新；
   - 任何功能/发布分支在合并到 `main` 前，必须显式丢弃本分支携带的 `data/index.json` 改动：
     `git checkout origin/main -- data/index.json`；
   - 发布前运行 `scripts/verify-index-before-merge.sh` 验证 `latest` 字段与最新 issue 文件一致；
   - 普通功能卡禁止在实现阶段修改 `data/index.json`。

---

## 验证怎么做才算数

**唯一的权威验证是打真线上地址看首屏。** 现成的脚本：

```bash
node ~/.hermes/scripts/artbook-prod-smoke.mjs
```

它用真 Playwright 浏览器、移动视口 390×844、全新 context（无 SW 无缓存，
等价于删掉重装后第一次打开）、只看首屏不做任何导航，断言三条：
首屏有真实内容 / 零 404 零未捕获 JS 错误 / 图标不是占位圆圈。
全过则静默退出 0，有问题则打印明细。

**交付说明里贴这个脚本的输出，比贴任何 curl 和毫秒数都有说服力。**

只有赤拔本人能提供的证据（iPhone 真机安装、主屏启动、离线实测），
自动化替不了——需要就 `kanban_block(kind=needs_input)` 上报，
**不要拿自制演示页、模拟主屏图、无头浏览器截图冒充真机截图**。
