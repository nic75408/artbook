{
  "name": "artbook",
  "version": "1.0.0",
  "description": "Artbook PWA",
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start-test": "http-server -p 8080 .",
    "test:pwa": "node evidence/pwa-acceptance-test.js",
    "test:feed": "npx playwright test tests/feed-alignment.spec.js --project=chromium",
    "test:aspect": "npx playwright test tests/artwork-aspect-ratio.spec.js --project=chromium"
  }
}
